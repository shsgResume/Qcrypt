

vector<int> keyGen(void){

    vector<int> keys;

    //choose two large primes

    //compute the modulus

    //compute the carmichael totient function

    //get the encryption integer

    //get the decryption integer

    return keys;
}


void distribution(vector<int> keys){

    return 0;
}

void encryption(std::string message){

    std::string encryptedMessage;

    return encryptedMessage;
}

void decryption(std::string encryptedMessage){

    std::string decryptedMessage;

    return decryptedMessage;
}